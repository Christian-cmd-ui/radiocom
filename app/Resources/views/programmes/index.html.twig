{% extends 'base_admin.html.twig' %}

{% block content %}
    <h1>Liste des programmes disponibles</h1>

    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Heures</th>
                <th>Nom du programme</th>
                <th>Emission</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for programme in programmes %}
            <tr>
                <td><a href="{{ path('programmes_show', { 'id': programme.id }) }}">{{ programme.id }}</a></td>
                <td>{{ programme.heures }}</td>
                <td>{{ programme.nom }}</td>
                <td>{{ programme.emission }}</td>
                <td>
                    <ul>
                        <li>
                            <a  class="btn btn-dark btn-custom waves-effect waves-light" href="{{ path('programmes_show', { 'id': programme.id }) }}">Détails</a>
                        </li>
                        <li>
                            <a class="btn btn-dark btn-custom waves-effect waves-light" href="{{ path('programmes_edit', { 'id': programme.id }) }}">Modifier</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a class="btn btn-dark btn-custom waves-effect waves-light" href="{{ path('programmes_new') }}">Ajouter un nouveau programme</a>
        </li>
    </ul>
       {% block javascript %}
        <script>
        $('.nav-item.nav-link')[4].addClass('active');
        </script>
    {% endblock %}
{% endblock %}

{% extends 'base_admin.html.twig' %}

{% block content %}
    <h1>Donateur</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ donateur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ donateur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ donateur.prenom }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ donateur.sexe }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ donateur.email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ donateur.phone }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ donateur.montant }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a class="btn btn-dark btn-custom waves-effect waves-light" href="{{ path('donateurs_index') }}">retourner à la liste des donateurs</a>
        </li>
        <li>
            <a class="btn btn-dark btn-custom waves-effect waves-light" href="{{ path('donateurs_edit', { 'id': donateur.id }) }}">Modifier </a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

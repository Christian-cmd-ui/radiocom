{% extends 'base_admin.html.twig' %}

{% block content %}
    <h1>Liste des formations disponibles</h1>

    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Objectifs</th>
                <th>Duree</th>
                <th>Modaliteparticipation</th>
                <th>Type</th>
                <th>Nombreplace</th>
                <th>Lieu</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for formation in formations %}
            <tr>
                <td><a href="{{ path('formations_show', { 'id': formation.id }) }}">{{ formation.id }}</a></td>
                <td>{{ formation.libelle }}</td>
                <td>{{ formation.objectifs }}</td>
                <td>{{ formation.duree }}</td>
                <td>{{ formation.modaliteparticipation }}</td>
                <td>{{ formation.type }}</td>
                <td>{{ formation.nombreplace }}</td>
                <td>{{ formation.lieu }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('formations_show', { 'id': formation.id }) }}">Détails</a>
                        </li>
                        <li>
                            <a href="{{ path('formations_edit', { 'id': formation.id }) }}">Modifier</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a class="btn btn-dark btn-custom waves-effect waves-light" href="{{ path('formations_new') }}">Create a new formation</a>
        </li>
    </ul>
      {% block javascript %}
    <script>
        $('.nav-item.nav-link')[3].addClass('active');
    </script>
    {% endblock %}
{% endblock %}
